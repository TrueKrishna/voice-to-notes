# Example environment variables
# Copy to .env and fill in your values

# Optional: Pre-set your Gemini API key
# You can also add keys through the web UI
# GEMINI_API_KEY=your-api-key-here

# Optional: Use PostgreSQL instead of SQLite
# DATABASE_URL=postgresql://user:password@localhost:5432/voice_notes

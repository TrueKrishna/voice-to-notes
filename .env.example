# =============================================================================
# Voice-to-Notes — Configuration
# Copy this file to .env and fill in your values.
# =============================================================================

# =========================
# DOCKER SETUP
# =========================

# Data directory for SQLite databases and uploaded files
# This is where your voice notes data will be stored persistently
# Default: ~/voice-notes-data (outside the project folder for safety)
# DATA_DIR=~/voice-notes-data

# Host path to your Google Drive root (or any parent directory).
# This is mounted into the container at /data/gdrive.
# The Settings UI lets you pick subdirectories within this mount.
GDRIVE_MOUNT_PATH=/Users/yourname/Google Drive

# =========================
# API KEYS
# =========================

# Gemini API keys — comma-separated for automatic rotation
# Get keys from: https://aistudio.google.com/apikey
# You can also add keys via the web UI at /keys
# GEMINI_API_KEYS=AIza...,AIza...
# GEMINI_API_KEY=AIza...

# =========================
# OPTIONAL OVERRIDES
# =========================
# Most settings below can be configured via the web UI at /settings.
# Env vars serve as defaults; DB settings (set via UI) take priority.

# Database URL (default: SQLite)
# DATABASE_URL=postgresql://user:password@localhost:5432/voice_notes

# V2 Engine — these are fallback defaults if not set in Settings UI
# LOCAL_SYNC_AUDIO_DIR=/data/gdrive/VoiceInbox
# OBSIDIAN_VAULT_DIR=/data/gdrive/ObsidianVault
# OBSIDIAN_NOTE_SUBDIR=VoiceNotes
# PROCESSING_MODE=personal_note
# GEMINI_MODEL=gemini-2.5-flash-preview-05-20
# STABILITY_SECONDS=10
# SCAN_INTERVAL=5
# AUDIO_BITRATE=48k

# Internal directories (rarely need changing)
# PROCESSING_TEMP_DIR=./data/engine/processing
# FAILED_DIR=./data/engine/failed
# REGISTRY_DB_PATH=./data/engine/registry.db
# ENGINE_VERSION=2.0.0
